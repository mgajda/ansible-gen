# LOCAL:
# Copy id_rsa, id_rsa.pub
ssh $TARGET -- << ENDSSH
# REMOTE:
chmod go-rwx .ssh/*
# Setup bash env
export HISTFILESIZE=1000
export HISTSIZE=1000
# With ROOT:
# Update all packages
sudo -s <<ENDROOT
# Mount EBS volume
sudo mkdir /media/scraped
sudo chown ubuntu:ubuntu /media/scraped
sudo chown -R ubuntu:ubuntu /media/scraped
ENDROOT # ALL with ROOT
# Clone Git repo
exit # check that exit or logout has been issued at the end (or warn)
ENDSSH

